const {getMagnetsFromAnimeName, getShowsOnly, getNumberOfShows} = require('./get')
const {getLatest} = require('./latest')

module.exports = {
  getLatest,
  getMagnetsFromAnimeName,
  getNumberOfShows,
  getShowsOnly
}
